# Copy to scripts/migrate/.env (gitignored). Names only; never commit values.

# AWS (local profile credentials are used; no keys here)
APP_REGION=ap-southeast-1
IT_TABLE_NAME=
PHOTO_TABLE_NAME=
MEDIA_BUCKET=
MEDIA_BASE_URL=

# it-portfolio Supabase project (service role key: read-only use)
IT_SUPABASE_URL=
IT_SUPABASE_SERVICE_KEY=

# photography-portfolio Supabase project
PHOTO_SUPABASE_URL=
PHOTO_SUPABASE_SERVICE_KEY=

# Cloudinary (photography-portfolio) — Admin API listing only
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
